name: CI

on:
  pull_request:
  push:
    branches:
      - main
    paths:
      - "plugins/**"
      - "build.mjs"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/ci.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm

      - name: Install locked dependencies
        run: npm ci --no-audit --prefer-offline

      - name: Audit shipped dependencies
        # vendetta-types pulls Metro/image-size only as build-time dev tooling.
        # GitHub currently lists no patched image-size release for its DoS advisories;
        # Trivy still scans the full lockfile, while this gate protects shipped deps.
        run: npm audit --omit=dev --audit-level=high

      - name: Build plugins
        run: npm run build
